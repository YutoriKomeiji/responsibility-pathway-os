# RPOS 0.1.0a3 release candidate manifest

Status: `CANDIDATE_PREPARATION / NOT_PUBLISHED / HUMAN_GATE_REQUIRED`

Owner: Linear DAN-111

## Exact published baseline

The published `0.1.0a2` release-candidate workflow ran successfully from:

- source commit: `fd0deda56e2903d50ae2053caf73aba2711768ea`
- workflow: `RPOS release candidate`
- workflow run: `33236397405`
- published package: `responsibility-pathway-os==0.1.0a2`

Published artifact provenance retained in `CHANGELOG.md`:

- wheel SHA256: `83251e34ce847858b4a93535d1f309abdb125587d1b4d184876cce76203f9a31`
- sdist SHA256: `614472025540db6c4dff228d0ab49d1160b24668c0bb940d5352b078f2915ea2`

## Candidate source baseline

Candidate preparation starts from current `main`:

- source commit: `4d8e68b1cb186f4d0c23155ac4d2be5ee14a4553`
- latest semantic change: additive Responsibility Routing runtime classifier from DAN-96

Formal comparison interval:

`fd0deda56e2903d50ae2053caf73aba2711768ea...4d8e68b1cb186f4d0c23155ac4d2be5ee14a4553`

Fresh compare result:

- `70` commits ahead of the exact a2 publication source;
- `26` changed files;
- no commits behind the published source.

## Version classification

Preferred candidate: `0.1.0a3`.

Reason:

- the existing v0.1 operation-state contract remains in place;
- Responsibility Routing is added as a classifier/API surface rather than replacing the existing state machine;
- legacy `HumanReturnPackage` remains available for compatibility;
- new public symbols are additive: `ResponsibilityRoute`, `ResponsibilityRouteKind`, `classify_responsibility_route`;
- post-a2 integration demos and public-surface changes expand evidence and usability without requiring a breaking public-API reset;
- no observed evidence at this checkpoint requires a breaking alpha-version reset.

This version remains a candidate until exact-head validation passes.

## Exact post-a2 file delta

### A. Package / runtime semantic delta

- `src/rpos/routing.py` — added bounded Responsibility Routing classifier and route envelope;
- `src/rpos/__init__.py` — exports routing symbols.

Primary semantic effect:

- unresolved external-effect states can be classified as reconciliation hold rather than generic Human Return;
- repair and return-for-authorization remain distinct routes;
- Human Gate is a bounded route;
- route selection does not grant Authority.

### B. Executable evidence / test delta

- `tests/test_responsibility_routing.py` — added route semantics tests;
- `examples/production_grade_demos/external_service.py` — added deterministic external-service fixture;
- `examples/production_grade_demos/run_demo.py` — added integration scenarios;
- `examples/production_grade_demos/README.md` — added demo scope and evidence boundaries;
- `tests/test_production_grade_demos.py` — added executable demo tests;
- `tests/test_product_status.py` — updated public-state assertions.

These are scoped engineering evidence. They do not create legal/organizational Authority or universal deployment correctness.

### C. Public documentation / support / product-surface delta

Changed or added:

- `README.md`
- `README.ja.md`
- `SUPPORT.md`
- `docs/en/current-scope-and-extension-surface.md`
- `docs/en/responsibility-routing-current-source.md`
- `docs/en/support-maturity.md`
- `docs/ja/claim-boundary-promotion.md`
- `docs/ja/commit-time-authority-revalidation.md`
- `docs/ja/current-scope-and-extension-surface.md`
- `docs/ja/operational-product-experience.md`
- `docs/ja/responsibility-routing-current-source.md`
- `formal/lean/README.ja.md`
- `site/app.js`
- `site/demo.html`
- `site/index.html`
- `site/ja.html`

These changes include B2B/public-entry refinement, support-maturity clarification, Japanese localization, current-main integration evidence, and Responsibility Routing semantics.

### D. Release metadata / historical-state delta

- `CHANGELOG.md`
- `product-status.json`

Current release workflow remains PyPI-centered Trusted Publishing and does not create GitHub Releases. An empty GitHub Releases collection is therefore consistent with current release architecture.

Do not add a GitHub Release surface merely for symmetry. A change to that policy requires an explicit release-design decision.

Before exact-head candidate validation, reconcile at minimum:

- `pyproject.toml` candidate version;
- `README.md` / `README.ja.md` candidate versus latest-published wording;
- `product-status.json` candidate version with `publication_state: not_published` while retaining `latest_published_version: 0.1.0a2`;
- `CHANGELOG.md` candidate entry;
- `.github/workflows/release.yml` expected candidate version;
- exact-version/publication-state tests;
- site/docs only where they intentionally describe the candidate rather than the still-published a2 artifact.

### E. Historical evidence that must remain immutable

Do not rewrite:

- a2 workflow source identity;
- a2 wheel/sdist hashes;
- a2 publication date/state as historical fact;
- prior release records as if a3 had already shipped.

## Semantic boundaries for 0.1.0a3

The candidate must preserve:

```text
human_in_the_loop != responsibility_handling_complete
fail_closed != human_gate
route_selection != authority_grant
receiver_capability != receiver_eligibility
evidence_transfer != authority_transfer
receipt != verified_external_effect
repair_readiness != resume_authority
state_recovery != authorization_recovery
```

Responsibility-processing delegation does not itself transfer final responsibility or legal/organizational Authority.

## Publication-safety invariants

Before any `publish=true` action, all of the following must be simultaneously true at the **same exact commit**:

1. package candidate version is exactly `0.1.0a3`;
2. candidate state is explicitly `not_published` before publication;
3. `latest_published_version` remains `0.1.0a2` until public readback proves a3 exists;
4. README EN/JA distinguish the a3 candidate from the currently published a2 artifact;
5. CHANGELOG does not claim a3 publication before it occurs;
6. release workflow is run first with `publish=false`;
7. wheel/sdist metadata match the expected version;
8. standalone, OS/Python compatibility, Lean, clean-install, CLI, SBOM, public-export and hash evidence all bind to the same HEAD;
9. no article or product page says a3 is public before PyPI readback;
10. a Human Gate is obtained immediately before the irreversible publication action.

After authorized publication:

1. read PyPI back independently;
2. verify version, filenames, hashes and Trusted Publisher metadata;
3. only then promote `latest_published_version` to `0.1.0a3` and `publication_state` to `pypi_published`;
4. reconcile README/site/article links and current/historical surfaces;
5. rerun public readback before declaring the transition closed.

## Exact-head validation target

Before any publication decision, the candidate must pass the repository's release-candidate evidence chain at one exact commit, including:

- standalone Python tests;
- Ubuntu/Windows Python compatibility matrix;
- wheel/sdist build and clean-install checks;
- CLI smoke checks;
- machine-checked Lean project and exact-head formal assurance manifest;
- public-export manifest;
- SBOM;
- source-bound artifact hash bundle;
- current public-surface consistency review.

## Human Gate

Allowed before Human Gate:

- branch and PR work;
- version-candidate preparation;
- documentation and migration notes;
- tests and exact-head validation;
- `publish=false` workflow execution;
- merge of internally validated release preparation where repository policy permits.

Requires explicit Master approval:

- PyPI publication;
- any newly introduced GitHub tag/Release publication surface;
- external announcement/article publication that states the release is public.

Publication success alone does not close the version transition. Public readback and post-release reconciliation are required after any authorized publication.
